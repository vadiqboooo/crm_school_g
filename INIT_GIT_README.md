# Инициализация Git (без node_modules)

## 🚀 Автоматическая инициализация

### Windows:
```cmd
# Просто запустите файл
init_git.bat
```

### Linux/Mac:
```bash
# Дайте права на выполнение
chmod +x init_git.sh

# Запустите скрипт
./init_git.sh
```

Скрипт автоматически:
- ✅ Инициализирует Git
- ✅ Проверит .gitignore
- ✅ Добавит все файлы
- ✅ **Удалит node_modules из индекса**
- ✅ **Удалит .env файлы из индекса**
- ✅ Создаст первый коммит

---

## 🔧 Ручная инициализация

Если скрипт не работает, выполните команды вручную:

### 1. Инициализируйте Git
```bash
git init
```

### 2. Добавьте файлы
```bash
git add .
```

### 3. Удалите node_modules из Git
```bash
# Если node_modules уже попал в Git
git rm -r --cached client_crm/node_modules
git rm -r --cached node_modules

# Для Windows используйте обратные слеши:
git rm -r --cached client_crm\node_modules
```

### 4. Удалите .env из Git
```bash
# Если .env попал в Git
git rm --cached server/.env
git rm --cached .env

# Для Windows:
git rm --cached server\.env
```

### 5. Пересоздайте индекс
```bash
git add .
```

### 6. Проверьте что добавляется
```bash
# Проверьте список файлов
git status

# Убедитесь что НЕТ:
# - node_modules/
# - .env файлов
```

### 7. Создайте коммит
```bash
git commit -m "Initial commit: CRM School project"
```

---

## 🌐 Подключение к GitHub/GitLab

После инициализации подключите удаленный репозиторий:

### GitHub:
```bash
# Создайте пустой репозиторий на github.com
# Затем выполните:
git remote add origin https://github.com/ваш-username/crm-school.git
git branch -M main
git push -u origin main
```

### GitLab:
```bash
git remote add origin https://gitlab.com/ваш-username/crm-school.git
git branch -M main
git push -u origin main
```

---

## ❓ Проблемы и решения

### node_modules всё равно в Git?

```bash
# Проверьте что node_modules в .gitignore
cat .gitignore | grep node_modules

# Если нет, добавьте:
echo "node_modules/" >> .gitignore

# Удалите из Git
git rm -r --cached client_crm/node_modules
git add .gitignore
git commit -m "fix: remove node_modules from git"
```

### .env файл в Git?

```bash
# НИКОГДА не коммитьте .env файлы!
# Удалите из Git:
git rm --cached server/.env

# Убедитесь что в .gitignore:
cat .gitignore | grep "\.env"

# Если нет, добавьте:
echo ".env" >> .gitignore

git add .gitignore
git commit -m "fix: remove .env from git"
```

### Большой размер репозитория?

Если node_modules уже закоммичен и репозиторий большой:

```bash
# Используйте git filter-branch (осторожно!)
git filter-branch --tree-filter 'rm -rf client_crm/node_modules' HEAD

# Или используйте BFG Repo-Cleaner
# https://rtyley.github.io/bfg-repo-cleaner/
```

---

## ✅ Проверка перед push

Перед отправкой в удаленный репозиторий проверьте:

```bash
# 1. Размер репозитория (должен быть небольшой)
du -sh .git

# 2. Список файлов в последнем коммите
git ls-files

# 3. Убедитесь что нет:
git ls-files | grep node_modules  # Должно быть пусто
git ls-files | grep "\.env$"      # Должно быть пусто

# 4. Проверьте что есть .env.example
git ls-files | grep ".env.example"  # Должен быть
```

---

## 📋 Что ДОЛЖНО быть в Git:

✅ Исходный код (.py, .ts, .tsx, .js)
✅ Конфигурационные файлы (package.json, requirements.txt)
✅ Документация (.md файлы)
✅ .env.example (шаблон без секретов)
✅ .gitignore и .gitattributes
✅ Миграции базы данных (alembic/versions/*.py)

## 📋 Что НЕ должно быть в Git:

❌ node_modules/ (зависимости Node.js)
❌ venv/ (виртуальное окружение Python)
❌ .env (секретные переменные)
❌ __pycache__/ (кэш Python)
❌ dist/, build/ (сборки)
❌ *.log (логи)
❌ *.db, *.sqlite (базы данных)
❌ .DS_Store, Thumbs.db (системные файлы)

---

## 🎯 Итоговая проверка

```bash
# Размер репозитория не должен превышать 10-20 MB
du -sh .git

# Количество файлов (примерно 100-300 без node_modules)
git ls-files | wc -l

# Если больше 5000 файлов - вероятно node_modules попал!
```

---

Готово! Теперь ваш репозиторий чистый и готов к публикации! 🎉
