# 🔌 Подключение к PostgreSQL через pgAdmin

## 📋 Содержание

1. [Настройка PostgreSQL на Ubuntu](#настройка-postgresql-на-ubuntu)
2. [Установка pgAdmin](#установка-pgadmin)
3. [Подключение через pgAdmin](#подключение-через-pgadmin)
4. [Troubleshooting](#troubleshooting)

---

## 🔧 Настройка PostgreSQL на Ubuntu

### Шаг 1: Проверить статус PostgreSQL

```bash
# Проверить, что PostgreSQL запущен
sudo systemctl status postgresql

# Если не запущен, запустить
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### Шаг 2: Установить пароль для пользователя postgres

```bash
# Войти в PostgreSQL
sudo -u postgres psql

# В psql консоли выполнить:
ALTER USER postgres WITH PASSWORD 'ваш_пароль_здесь';

# Выйти
\q
```

### Шаг 3: Настроить удаленное подключение (если pgAdmin на другом компьютере)

#### 3.1. Редактировать postgresql.conf

```bash
# Найти файл конфигурации
sudo -u postgres psql -c "SHOW config_file;"

# Обычно находится в:
# /etc/postgresql/15/main/postgresql.conf

# Редактировать файл
sudo nano /etc/postgresql/16/main/postgresql.conf
```

**Найти и изменить строку:**

```conf
# Было:
#listen_addresses = 'localhost'

# Стало (для всех IP):
listen_addresses = '*'

# Или для конкретного IP:
listen_addresses = '192.168.1.100,localhost'
```

#### 3.2. Редактировать pg_hba.conf

```bash
# Редактировать файл
sudo nano /etc/postgresql/16/main/pg_hba.conf
```

**Добавить в конец файла:**

```conf
# Разрешить подключение с любого IP (для разработки)
host    all             all             0.0.0.0/0               md5

# Или только с конкретной подсети
host    all             all             192.168.1.0/24          md5

# Или только с конкретного IP
host    all             all             192.168.1.100/32        md5
```

**Объяснение:**
- `host` - тип подключения (TCP/IP)
- `all` - все базы данных
- `all` - все пользователи
- `0.0.0.0/0` - любой IP адрес
- `md5` - метод аутентификации (по паролю)

#### 3.3. Перезапустить PostgreSQL

```bash
# Перезапустить сервис
sudo systemctl restart postgresql

# Проверить статус
sudo systemctl status postgresql
```

### Шаг 4: Открыть порт в firewall

```bash
# Если используете UFW
sudo ufw allow 5432/tcp

# Проверить правила
sudo ufw status

# Если используете iptables
sudo iptables -A INPUT -p tcp --dport 5432 -j ACCEPT
sudo iptables-save
```

### Шаг 5: Узнать IP адрес сервера

```bash
# Узнать IP адрес
ip addr show

# Или
hostname -I

# Или внешний IP
curl ifconfig.me
```

---

## 💻 Установка pgAdmin

### Вариант 1: pgAdmin 4 для Windows

1. Скачать с официального сайта: https://www.pgadmin.org/download/
2. Запустить установщик
3. Следовать инструкциям установки

### Вариант 2: pgAdmin 4 для Linux (Ubuntu)

```bash
# Установить pgAdmin4
curl -fsS https://www.pgadmin.org/static/packages_pgadmin_org.pub | sudo gpg --dearmor -o /usr/share/keyrings/packages-pgadmin-org.gpg

sudo sh -c 'echo "deb [signed-by=/usr/share/keyrings/packages-pgadmin-org.gpg] https://ftp.postgresql.org/pub/pgadmin/pgadmin4/apt/$(lsb_release -cs) pgadmin4 main" > /etc/apt/sources.list.d/pgadmin4.list'

sudo apt update
sudo apt install pgadmin4-desktop

# Или веб-версия
sudo apt install pgadmin4-web
sudo /usr/pgadmin4/bin/setup-web.sh
```

### Вариант 3: Веб-версия через Docker (рекомендуется)

```bash
# Запустить pgAdmin в Docker
docker run -d \
  --name pgadmin \
  -p 5050:80 \
  -e PGADMIN_DEFAULT_EMAIL=admin@admin.com \
  -e PGADMIN_DEFAULT_PASSWORD=admin \
  dpage/pgadmin4

# Открыть в браузере: http://localhost:5050
# Email: admin@admin.com
# Password: admin
```

---

## 🔗 Подключение через pgAdmin

### Шаг 1: Открыть pgAdmin

Запустите pgAdmin 4 на вашем компьютере.

### Шаг 2: Создать новое подключение

1. **Нажать правой кнопкой на "Servers"** в левой панели
2. Выбрать **"Register" → "Server..."**

### Шаг 3: Заполнить вкладку "General"

```
Name: CRM School (или любое название)
Server group: Servers
Comments: Production database (опционально)
```

### Шаг 4: Заполнить вкладку "Connection"

#### Для локального подключения (pgAdmin на том же сервере):

```
Host name/address: localhost
Port: 5432
Maintenance database: postgres
Username: postgres
Password: ваш_пароль
Save password: ✓ (отметить)
```

#### Для удаленного подключения:

```
Host name/address: 192.168.1.100 (или домен: crm.garryschool.ru)
Port: 5432
Maintenance database: postgres
Username: postgres
Password: ваш_пароль
Save password: ✓
```

#### Для Docker контейнера:

```
Host name/address: localhost (или IP сервера)
Port: 5432
Maintenance database: crm_school
Username: postgres
Password: ваш_пароль_из_.env
Save password: ✓
```

### Шаг 5: Настроить вкладку "SSL" (опционально)

Для безопасного подключения:

```
SSL mode: Require
```

### Шаг 6: Нажать "Save"

Подключение будет создано и появится в списке серверов.

### Шаг 7: Подключиться

1. Раскрыть сервер в дереве слева
2. Увидите базу данных **"crm_school"**
3. Раскрыть → **Schemas** → **public** → **Tables**

---

## 🎯 Пример подключения

### Локальное подключение (PostgreSQL на Ubuntu)

```
┌─────────────────────────────────────┐
│     General                         │
├─────────────────────────────────────┤
│ Name: CRM School Local              │
│ Server group: Servers               │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│     Connection                      │
├─────────────────────────────────────┤
│ Host: localhost                     │
│ Port: 5432                          │
│ Database: crm_school                │
│ Username: postgres                  │
│ Password: ●●●●●●●●                  │
│ ☑ Save password                     │
└─────────────────────────────────────┘
```

### Удаленное подключение (через интернет)

```
┌─────────────────────────────────────┐
│     General                         │
├─────────────────────────────────────┤
│ Name: CRM School Production         │
│ Server group: Servers               │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│     Connection                      │
├─────────────────────────────────────┤
│ Host: crm.garryschool.ru            │
│ Port: 5432                          │
│ Database: crm_school                │
│ Username: postgres                  │
│ Password: ●●●●●●●●                  │
│ ☑ Save password                     │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│     SSL                             │
├─────────────────────────────────────┤
│ SSL mode: Require                   │
└─────────────────────────────────────┘
```

### Docker контейнер

```
┌─────────────────────────────────────┐
│     General                         │
├─────────────────────────────────────┤
│ Name: CRM School Docker             │
│ Server group: Servers               │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│     Connection                      │
├─────────────────────────────────────┤
│ Host: localhost (или IP сервера)    │
│ Port: 5432                          │
│ Database: crm_school                │
│ Username: postgres                  │
│ Password: (из .env файла)           │
│ ☑ Save password                     │
└─────────────────────────────────────┘
```

---

## 🔍 Проверка подключения

### Из командной строки Ubuntu

```bash
# Проверить подключение локально
psql -U postgres -d crm_school -c "SELECT version();"

# Проверить удаленное подключение
psql -h localhost -U postgres -d crm_school -c "SELECT version();"

# Проверить, слушает ли PostgreSQL на нужном порту
sudo netstat -plnt | grep 5432

# Или
sudo ss -tlnp | grep 5432

# Проверить, какие IP слушает PostgreSQL
sudo -u postgres psql -c "SHOW listen_addresses;"
```

### Тестовое подключение через telnet

```bash
# Установить telnet (если нет)
sudo apt install telnet

# Проверить доступность порта
telnet localhost 5432

# Если подключение успешно, увидите:
# Trying 127.0.0.1...
# Connected to localhost.
# Escape character is '^]'.

# Нажать Ctrl+] и ввести quit
```

---

## 🆘 Troubleshooting

### Проблема 1: "Could not connect to server"

**Причина:** PostgreSQL не слушает на нужном IP/порту

**Решение:**

```bash
# Проверить listen_addresses
sudo -u postgres psql -c "SHOW listen_addresses;"

# Должно быть: * или конкретный IP
# Если localhost - изменить в postgresql.conf
sudo nano /etc/postgresql/15/main/postgresql.conf

# Найти и изменить:
listen_addresses = '*'

# Перезапустить
sudo systemctl restart postgresql
```

### Проблема 2: "Connection refused"

**Причина:** Firewall блокирует порт 5432

**Решение:**

```bash
# Открыть порт в UFW
sudo ufw allow 5432/tcp
sudo ufw reload

# Проверить
sudo ufw status

# Проверить, что порт открыт
sudo netstat -plnt | grep 5432
```

### Проблема 3: "Password authentication failed"

**Причина:** Неправильный пароль или метод аутентификации

**Решение:**

```bash
# Сбросить пароль
sudo -u postgres psql

# В psql:
ALTER USER postgres WITH PASSWORD 'новый_пароль';
\q

# Проверить pg_hba.conf
sudo nano /etc/postgresql/15/main/pg_hba.conf

# Убедиться что метод md5, а не peer:
# Было:
local   all             postgres                                peer

# Стало:
local   all             postgres                                md5

# Перезапустить
sudo systemctl restart postgresql
```

### Проблема 4: "no pg_hba.conf entry for host"

**Причина:** IP не разрешен в pg_hba.conf

**Решение:**

```bash
# Редактировать pg_hba.conf
sudo nano /etc/postgresql/15/main/pg_hba.conf

# Добавить в конец:
host    all             all             0.0.0.0/0               md5

# Или для конкретного IP:
host    all             all             192.168.1.100/32        md5

# Перезапустить
sudo systemctl restart postgresql
```

### Проблема 5: "Timeout occurred"

**Причина:** Сетевые проблемы или firewall

**Решение:**

```bash
# Проверить доступность сервера
ping 192.168.1.100

# Проверить порт
telnet 192.168.1.100 5432

# Проверить firewall на сервере
sudo ufw status

# Проверить iptables
sudo iptables -L -n | grep 5432
```

### Проблема 6: Docker контейнер недоступен

**Причина:** Порт не проброшен или контейнер не запущен

**Решение:**

```bash
# Проверить что контейнер запущен
docker compose ps

# Проверить порты
docker compose ps postgres

# Должен быть: 0.0.0.0:5432->5432/tcp

# Если порта нет, проверить docker-compose.yml:
services:
  postgres:
    ports:
      - "5432:5432"  # <-- эта строка

# Перезапустить
docker compose restart postgres
```

---

## 🔒 Безопасность

### Рекомендации для продакшена:

1. **Не используйте 0.0.0.0/0** в pg_hba.conf
   ```conf
   # Плохо (доступ откуда угодно):
   host    all    all    0.0.0.0/0    md5

   # Хорошо (только с конкретных IP):
   host    all    all    192.168.1.100/32    md5
   host    all    all    10.0.0.0/8          md5
   ```

2. **Используйте сильные пароли**
   ```bash
   # Сгенерировать случайный пароль
   openssl rand -base64 32
   ```

3. **Используйте SSL**
   ```bash
   # В postgresql.conf
   ssl = on
   ssl_cert_file = '/etc/ssl/certs/server.crt'
   ssl_key_file = '/etc/ssl/private/server.key'
   ```

4. **Ограничьте доступ через firewall**
   ```bash
   # Только с конкретного IP
   sudo ufw allow from 192.168.1.100 to any port 5432
   ```

5. **Используйте отдельного пользователя (не postgres)**
   ```sql
   -- Создать пользователя
   CREATE USER crm_admin WITH PASSWORD 'strong_password';

   -- Дать права
   GRANT ALL PRIVILEGES ON DATABASE crm_school TO crm_admin;

   -- Подключаться через этого пользователя
   ```

---

## 📊 Проверка успешного подключения

После подключения в pgAdmin вы должны увидеть:

```
Servers
└── CRM School
    ├── Databases (1)
    │   └── crm_school
    │       ├── Schemas
    │       │   └── public
    │       │       └── Tables (20+)
    │       │           ├── students
    │       │           ├── groups
    │       │           ├── lessons
    │       │           ├── weekly_reports
    │       │           └── ...
    │       └── ...
    └── ...
```

### Выполнить тестовый запрос:

1. Нажать правой кнопкой на базу **crm_school**
2. Выбрать **"Query Tool"**
3. Выполнить запрос:

```sql
-- Проверить версию
SELECT version();

-- Посмотреть список таблиц
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public';

-- Посмотреть количество студентов
SELECT COUNT(*) FROM students;

-- Посмотреть последние 5 студентов
SELECT * FROM students
ORDER BY created_at DESC
LIMIT 5;
```

---

## ✅ Чек-лист подключения

- [ ] PostgreSQL установлен и запущен
- [ ] Пароль для пользователя postgres установлен
- [ ] listen_addresses настроен в postgresql.conf
- [ ] pg_hba.conf разрешает подключения
- [ ] Порт 5432 открыт в firewall
- [ ] pgAdmin установлен
- [ ] Подключение в pgAdmin создано
- [ ] Тестовый запрос выполнен успешно
- [ ] Видны таблицы базы crm_school

---

## 📞 Дополнительная помощь

### Полезные команды:

```bash
# Посмотреть все настройки PostgreSQL
sudo -u postgres psql -c "SHOW ALL;"

# Найти конфигурационные файлы
sudo -u postgres psql -c "SHOW config_file;"
sudo -u postgres psql -c "SHOW hba_file;"

# Посмотреть логи PostgreSQL
sudo tail -f /var/log/postgresql/postgresql-15-main.log

# Перезапустить PostgreSQL
sudo systemctl restart postgresql

# Проверить статус
sudo systemctl status postgresql
```

---

**Готово! Теперь вы можете работать с базой данных через pgAdmin!** 🎉
